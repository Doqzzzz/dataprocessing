# 数据处理系统 — 演示解说词

## 演示数据

GDP 世界各国历年排名数据（1960–2024），含 6 列：年份、排名、国家/地区、洲、GDP（美元）、占世界百分比。约 1 万行。

---

## 解说词

### 第一步：加载数据

> 「首先打开网页，把 GDP 数据文件拖拽到上传区域，点击加载数据。系统自动识别文件编码，左侧显示文件信息——共一万多行、六列，右侧预览前 200 行数据。」

**操作：** 拖拽 `gdpallyear_allN.csv` → 点击「加载数据」

**展示要点：**
- 拖拽上传，自动识别 GBK 编码
- 左侧显示行数/列数
- 右侧表格预览数据

---

### 第二步：查看数据概况

> 「展开左下角的数据概况面板，可以看到每列的数据类型和缺失值情况。GDP 列是字符串格式，后面我们需要转换成数值。」

**操作：** 点击「数据概况」折叠面板

**展示要点：**
- 列名、类型、缺失值一览
- 发现 GDP 列是 object 类型

---

### 第三步：填充缺失值

> 「根据数据概况，我们发现某些列存在缺失值。先选中目标列，然后在缺失值处理区域点击填充缺失值，输入填充内容，确认执行。数据表格即时更新。」

**操作：** 选择目标列 → 点击「填充缺失值」→ 输入填充值 → 确认

**展示要点：**
- 分类操作按钮，一目了然
- 需要参数时弹出对话框
- 操作完成后提示成功

---

### 第四步：标准化处理

> 「接下来对 GDP 数据进行标准化处理。先选中 GDP 列，然后在数值变换区域点击标准化，系统自动计算 Z-score。标准化后数据均值接近 0，标准差为 1，方便后续建模分析。」

**操作：** 选中某数值列 → 点击「标准化 (Z-score)」

**展示要点：**
- 归一化/标准化一键完成
- 无需手动计算

---

### 第五步：排序查看

> 「我们想看 2024 年各国 GDP 排名情况。选择排名列，点击按此列升序，表格立刻重新排列。」

**操作：** 选中「排名」列 → 点击「按此列升序」

**展示要点：**
- 一键排序
- 表格即时更新

---

### 第六步：删除列

> 「洲这一列暂时不需要，选中后点击删除此列，该列就从数据集中移除了。」

**操作：** 选中列 → 点击「删除此列」

---

### 第七步：撤销操作

> 「如果删错了怎么办？点击撤销上一步，数据回到删除前的状态。系统保留了操作历史，可以多次回退。」

**操作：** 点击「撤销上一步」

**展示要点：**
- 按钮显示可撤销步数
- 回退后数据即时恢复

---

### 第八步：双击编辑单元格

> 「除了批量处理，也可以精确修改单个数据。双击任意单元格，输入新值，回车确认，修改即时保存。」

**操作：** 双击单元格 → 修改 → 回车

---

### 第九步：导出结果

> 「处理完成后，点击导出数据，可以选择 CSV 或 Excel 格式下载。所有处理结果都会保留在导出文件中。」

**操作：** 点击「导出数据」→ 选择格式 → 下载

---

## 演示总结

| 功能 | 对应操作 |
|------|----------|
| 数据加载 | 拖拽上传 CSV/Excel，自动识别编码 |
| 缺失值处理 | 填充缺失值 / 删除含缺失的行 |
| 数值变换 | 归一化 (0~1) / 标准化 (Z-score) |
| 文本处理 | 去除首尾空格 |
| 列管理 | 复制 / 排序 / 删除列 |
| 撤销 | 撤销上一步（多步回退） |
| 单元格编辑 | 双击直接修改 |
| 数据导出 | CSV / Excel 下载 |
